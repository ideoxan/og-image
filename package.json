{
    "name": "og-image",
    "version": "0.0.0",
    "devDependencies": {
        "prettier": "^2.8.3",
        "wrangler": "2.8.0"
    },
    "private": true,
    "scripts": {
        "start": "wrangler dev",
        "deploy": "wrangler publish",
        "format": "prettier --write ."
    },
    "dependencies": {
        "ezog": "^0.4.0"
    }
}
